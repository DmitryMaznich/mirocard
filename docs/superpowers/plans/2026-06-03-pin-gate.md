# PIN-гейт на запуск сессии — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Блокировать запуск любой сессии PIN-кодом — ребёнок не может начать занятие без взрослого.

**Architecture:** Кастомный PIN-пад (без системной клавиатуры) показывается поверх экрана каждый раз при нажатии «Начать занятие». При первом запуске — режим создания PIN. Хэш хранится в `settings.adultPinHash` в store и IndexedDB. Тап-код на «Завершить» снимается: на итогах — обычная кнопка, в модалке прерывания — чистый hold (без тапов).

**Tech Stack:** React, Zustand, Web Crypto API (`crypto.subtle.digest`), Vitest (jsdom), `@/core/db` + `kv` для персистентности

---

## Файловая карта

| Действие | Файл |
|---|---|
| Create | `src/shared/utils/pinHash.js` |
| Create | `src/shared/utils/pinHash.test.js` |
| Create | `src/shared/components/PinGateModal.jsx` |
| Create | `src/shared/components/PinGateModal.css` |
| Modify | `src/shared/components/HoldButton.jsx` |
| Modify | `src/features/session/SessionSummary.jsx` |
| Modify | `src/App.jsx` |
| Modify | `src/features/session/ParamsScreen.jsx` |
| Modify | `src/features/settings/SettingsScreen.jsx` |

---

## Task 1: Добавить `skipTaps` в HoldButton

**Files:**
- Modify: `src/shared/components/HoldButton.jsx`

- [ ] **Step 1: Обновить HoldButton**

Заменить сигнатуру и всю логику так, чтобы `skipTaps={true}` пропускал фазу тапов:

```jsx
// src/shared/components/HoldButton.jsx
import { useRef, useState, useEffect } from "react";

const TICK_MS       = 40;
const TAP_MAX_MS    = 400;
const TAP_WINDOW_MS = 2500;
const REQUIRED_TAPS = 3;

export default function HoldButton({ onAction, children, className = "", holdMs = 3000, style, skipTaps = false }) {
  const requiredTaps = skipTaps ? 0 : REQUIRED_TAPS;

  const [tapCount,  setTapCount]  = useState(0);
  const [phase,     setPhase]     = useState("tap");
  const [progress,  setProgress]  = useState(0);

  const intervalRef   = useRef(null);
  const resetTimerRef = useRef(null);
  const pressStartRef = useRef(null);
  const tapCountRef   = useRef(0);

  useEffect(() => () => {
    clearInterval(intervalRef.current);
    clearTimeout(resetTimerRef.current);
  }, []);

  function resetAll() {
    clearInterval(intervalRef.current);
    clearTimeout(resetTimerRef.current);
    intervalRef.current = null;
    tapCountRef.current = 0;
    setTapCount(0);
    setProgress(0);
    setPhase("tap");
  }

  function scheduleReset() {
    clearTimeout(resetTimerRef.current);
    resetTimerRef.current = setTimeout(resetAll, TAP_WINDOW_MS);
  }

  function startHold() {
    if (intervalRef.current) return;
    clearTimeout(resetTimerRef.current);
    const start = Date.now();
    intervalRef.current = setInterval(() => {
      const pct = Math.min(((Date.now() - start) / holdMs) * 100, 100);
      setProgress(pct);
      if (pct >= 100) {
        clearInterval(intervalRef.current);
        intervalRef.current = null;
        resetAll();
        onAction();
      }
    }, TICK_MS);
  }

  function cancelHold() {
    if (intervalRef.current) {
      clearInterval(intervalRef.current);
      intervalRef.current = null;
    }
    setProgress(0);
    scheduleReset();
  }

  function onPointerDown(e) {
    e.currentTarget.setPointerCapture(e.pointerId);
    pressStartRef.current = Date.now();
    if (tapCountRef.current >= requiredTaps) {
      startHold();
    }
  }

  function onPointerUp() {
    const duration = Date.now() - (pressStartRef.current ?? Date.now());

    if (tapCountRef.current >= requiredTaps) {
      cancelHold();
      return;
    }

    if (duration < TAP_MAX_MS) {
      tapCountRef.current += 1;
      setTapCount(tapCountRef.current);
      if (tapCountRef.current >= requiredTaps) {
        setPhase("hold");
      }
      scheduleReset();
    } else {
      resetAll();
    }
  }

  function onPointerLeave() {
    if (tapCountRef.current >= requiredTaps && intervalRef.current) {
      cancelHold();
    }
  }

  return (
    <button
      className={`hold-btn ${className}`}
      style={{ "--hold-p": progress, ...style }}
      onPointerDown={onPointerDown}
      onPointerUp={onPointerUp}
      onPointerLeave={onPointerLeave}
      onPointerCancel={onPointerLeave}
      onContextMenu={(e) => e.preventDefault()}
    >
      {children}
      {!skipTaps && tapCount > 0 && tapCount < REQUIRED_TAPS && (
        <span className="hold-btn-taps">
          {"●".repeat(tapCount)}{"○".repeat(REQUIRED_TAPS - tapCount)}
        </span>
      )}
    </button>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/shared/components/HoldButton.jsx
git commit -m "feat(holdbutton): add skipTaps prop to bypass tap phase"
```

---

## Task 2: Убрать тап-код с «Завершить» в SessionSummary

**Files:**
- Modify: `src/features/session/SessionSummary.jsx:12,190-195`

- [ ] **Step 1: Заменить HoldButton на Button на итогах**

Убрать импорт HoldButton, заменить элемент:

```jsx
// Строка 12 — убрать:
import HoldButton from "@/shared/components/HoldButton";

// Строки 190-195 — заменить:
// БЫЛО:
<HoldButton
  className="summary-finish-btn"
  onAction={() => setScreen("home")}
>
  <span className="summary-finish-btn__label">Завершить</span>
</HoldButton>

// СТАЛО:
<Button className="summary-finish-btn" onClick={() => setScreen("home")}>
  Завершить
</Button>
```

- [ ] **Step 2: Commit**

```bash
git add src/features/session/SessionSummary.jsx
git commit -m "feat(summary): replace tap-code HoldButton with plain Button"
```

---

## Task 3: Убрать тап-код с «Завершить» в модалке прерывания

**Files:**
- Modify: `src/App.jsx:244`

- [ ] **Step 1: Добавить `skipTaps` к HoldButton в exit-модалке**

Строка 244 в App.jsx — добавить проп `skipTaps`:

```jsx
// БЫЛО:
<HoldButton className="btn btn--danger" onAction={finishSessionFromPrompt}>Завершить</HoldButton>

// СТАЛО:
<HoldButton className="btn btn--danger" onAction={finishSessionFromPrompt} skipTaps>Завершить</HoldButton>
```

- [ ] **Step 2: Commit**

```bash
git add src/App.jsx
git commit -m "feat(exit-modal): remove tap-code from exit confirmation, keep hold"
```

---

## Task 4: Утилита хэширования PIN

**Files:**
- Create: `src/shared/utils/pinHash.js`
- Create: `src/shared/utils/pinHash.test.js`

- [ ] **Step 1: Написать failing тест**

```js
// src/shared/utils/pinHash.test.js
import { describe, it, expect } from "vitest";
import { hashPin, verifyPin } from "./pinHash";

describe("hashPin", () => {
  it("returns 64-char hex string", async () => {
    const hash = await hashPin("1234");
    expect(hash).toHaveLength(64);
    expect(hash).toMatch(/^[0-9a-f]+$/);
  });

  it("same pin → same hash", async () => {
    expect(await hashPin("5678")).toBe(await hashPin("5678"));
  });

  it("different pins → different hashes", async () => {
    expect(await hashPin("1234")).not.toBe(await hashPin("1235"));
  });
});

describe("verifyPin", () => {
  it("returns true for correct pin", async () => {
    const hash = await hashPin("9999");
    expect(await verifyPin("9999", hash)).toBe(true);
  });

  it("returns false for wrong pin", async () => {
    const hash = await hashPin("9999");
    expect(await verifyPin("0000", hash)).toBe(false);
  });
});
```

- [ ] **Step 2: Запустить тест — убедиться что падает**

```bash
npx vitest run src/shared/utils/pinHash.test.js
```

Expected: FAIL — `Cannot find module './pinHash'`

- [ ] **Step 3: Реализовать утилиту**

```js
// src/shared/utils/pinHash.js
export async function hashPin(pin) {
  const data = new TextEncoder().encode(pin);
  const buf  = await crypto.subtle.digest("SHA-256", data);
  return Array.from(new Uint8Array(buf))
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
}

export async function verifyPin(pin, storedHash) {
  return (await hashPin(pin)) === storedHash;
}
```

- [ ] **Step 4: Запустить тест — убедиться что проходит**

```bash
npx vitest run src/shared/utils/pinHash.test.js
```

Expected: PASS — 5 tests

- [ ] **Step 5: Commit**

```bash
git add src/shared/utils/pinHash.js src/shared/utils/pinHash.test.js
git commit -m "feat(pin): add SHA-256 pin hashing utility with tests"
```

---

## Task 5: Компонент PinGateModal

**Files:**
- Create: `src/shared/components/PinGateModal.jsx`
- Create: `src/shared/components/PinGateModal.css`

- [ ] **Step 1: Создать CSS**

```css
/* src/shared/components/PinGateModal.css */
.pin-gate-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.55);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

.pin-gate {
  background: #fff;
  border-radius: 20px;
  padding: 36px 28px 28px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
  width: 300px;
  box-shadow: 0 8px 32px rgba(0,0,0,0.18);
}

.pin-gate__title {
  font-size: 17px;
  font-weight: 600;
  color: #1a1a2e;
  text-align: center;
}

.pin-gate__dots {
  display: flex;
  gap: 18px;
}

.pin-dot {
  width: 18px;
  height: 18px;
  border-radius: 50%;
  border: 2px solid #c0c4cc;
  background: transparent;
  transition: background 0.12s, border-color 0.12s;
}

.pin-dot--filled {
  background: #5b8dee;
  border-color: #5b8dee;
}

@keyframes pin-shake {
  0%, 100% { transform: translateX(0); }
  20%       { transform: translateX(-10px); }
  40%       { transform: translateX(10px); }
  60%       { transform: translateX(-7px); }
  80%       { transform: translateX(7px); }
}

.pin-gate__dots--shake {
  animation: pin-shake 0.45s ease-in-out;
}

.pin-gate__error {
  font-size: 13px;
  color: #e53e3e;
  text-align: center;
  min-height: 18px;
}

.pin-pad {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
  width: 100%;
}

.pin-key {
  height: 60px;
  border-radius: 14px;
  border: none;
  background: #f2f4f8;
  font-size: 24px;
  font-weight: 500;
  color: #1a1a2e;
  cursor: pointer;
  transition: background 0.1s;
  -webkit-tap-highlight-color: transparent;
  user-select: none;
  touch-action: manipulation;
}

.pin-key:active {
  background: #d6e4ff;
}

.pin-key--empty {
  background: transparent;
  pointer-events: none;
}

.pin-key--back {
  font-size: 20px;
  color: #666;
}
```

- [ ] **Step 2: Создать компонент**

```jsx
// src/shared/components/PinGateModal.jsx
import { useState } from "react";
import { hashPin, verifyPin } from "@/shared/utils/pinHash";
import "./PinGateModal.css";

export default function PinGateModal({ pinHash, onSuccess, onSetPin }) {
  const isSetup = pinHash === null;

  const [stage,       setStage]       = useState("enter"); // "enter" | "confirm"
  const [digits,      setDigits]      = useState([]);
  const [firstDigits, setFirstDigits] = useState([]);
  const [shake,       setShake]       = useState(false);
  const [error,       setError]       = useState("");

  function triggerShake(msg) {
    setError(msg);
    setShake(true);
    setDigits([]);
    setTimeout(() => setShake(false), 500);
  }

  async function handleFourDigits(finalDigits) {
    const pin = finalDigits.join("");

    if (isSetup) {
      if (stage === "enter") {
        setFirstDigits(finalDigits);
        setStage("confirm");
        setDigits([]);
        setError("");
      } else {
        if (pin === firstDigits.join("")) {
          const hash = await hashPin(pin);
          onSetPin(hash);
          onSuccess();
        } else {
          triggerShake("PIN не совпадает. Начните заново.");
          setStage("enter");
          setFirstDigits([]);
        }
      }
    } else {
      const ok = await verifyPin(pin, pinHash);
      if (ok) {
        onSuccess();
      } else {
        triggerShake("Неверный PIN");
      }
    }
  }

  function pressDigit(d) {
    if (digits.length >= 4) return;
    setError("");
    const next = [...digits, d];
    setDigits(next);
    if (next.length === 4) {
      handleFourDigits(next);
    }
  }

  function pressBackspace() {
    setDigits((d) => d.slice(0, -1));
  }

  const title = isSetup
    ? (stage === "enter" ? "Придумайте PIN-код" : "Повторите PIN-код")
    : "Введите PIN-код";

  return (
    <div className="pin-gate-overlay">
      <div className="pin-gate">
        <div className="pin-gate__title">{title}</div>

        <div className={`pin-gate__dots${shake ? " pin-gate__dots--shake" : ""}`}>
          {[0, 1, 2, 3].map((i) => (
            <span key={i} className={`pin-dot${digits.length > i ? " pin-dot--filled" : ""}`} />
          ))}
        </div>

        <div className="pin-gate__error">{error}</div>

        <div className="pin-pad">
          {["1","2","3","4","5","6","7","8","9"].map((d) => (
            <button key={d} className="pin-key" onClick={() => pressDigit(d)}>{d}</button>
          ))}
          <button className="pin-key pin-key--empty" tabIndex={-1} />
          <button className="pin-key" onClick={() => pressDigit("0")}>0</button>
          <button className="pin-key pin-key--back" onClick={pressBackspace}>⌫</button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```bash
git add src/shared/components/PinGateModal.jsx src/shared/components/PinGateModal.css
git commit -m "feat(pin): add PinGateModal component with custom keypad"
```

---

## Task 6: Подключить PinGateModal в ParamsScreen

**Files:**
- Modify: `src/features/session/ParamsScreen.jsx`

Кнопка «Начать занятие» есть в двух местах (строки 577 и 617). Оба вызывают `startSession()`. Нужно добавить состояние модалки и разбить `startSession` на «открыть гейт» и «запустить сессию».

- [ ] **Step 1: Обновить ParamsScreen.jsx**

Добавить в импорты:
```jsx
import PinGateModal from "@/shared/components/PinGateModal";
import { getDb, kv } from "@/core/db";
```

Добавить в начало компонента `ParamsScreen` (после существующих `useAppStore` calls):
```jsx
const adultPinHash = useAppStore((s) => s.settings.adultPinHash);
const settings     = useAppStore((s) => s.settings);
const patchSettings = useAppStore((s) => s.patchSettings);

const [showPinGate, setShowPinGate] = useState(false);
```

Заменить функцию `startSession()` (строка 410) на две функции:

```jsx
function openPinGate() {
  if (isReading && !activeText) {
    setScreen("texts");
    return;
  }
  setShowPinGate(true);
}

function launchSession() {
  setShowPinGate(false);
  markSessionStart();
  persistStudentTopicLink(activeStudentId, activeTopicId, { params, videoRewardEnabled: videoReward, rewardThreshold });
  setScreen("session");
}

async function handleSetPin(hash) {
  patchSettings({ adultPinHash: hash });
  const db = await getDb();
  await kv.set(db, "settings", { ...settings, adultPinHash: hash });
}
```

Заменить оба вызова `onClick={startSession}` на `onClick={openPinGate}` (строки 577 и 617):
```jsx
// строка 577:
<Button fullWidth onClick={openPinGate} disabled={sentenceListEmpty}>Начать занятие</Button>
// строка 617:
<Button fullWidth onClick={openPinGate} disabled={sentenceListEmpty}>Начать занятие</Button>
```

Добавить модалку в JSX перед закрывающим `</div>` компонента (после блока `showModeInfo`):
```jsx
{showPinGate && (
  <PinGateModal
    pinHash={adultPinHash}
    onSuccess={launchSession}
    onSetPin={handleSetPin}
  />
)}
```

- [ ] **Step 2: Проверить что все тесты проходят**

```bash
npx vitest run
```

Expected: все существующие тесты PASS

- [ ] **Step 3: Commit**

```bash
git add src/features/session/ParamsScreen.jsx
git commit -m "feat(params): gate session start behind PIN modal"
```

---

## Task 7: Сброс PIN в Settings

**Files:**
- Modify: `src/features/settings/SettingsScreen.jsx`

- [ ] **Step 1: Читаем текущий экран настроек**

Прочитать `src/features/settings/SettingsScreen.jsx` (строки 30–110) чтобы найти место вставки нового блока — после существующих `adultConfirmAdvance`/`tapToAdvance` переключателей.

- [ ] **Step 2: Добавить блок сброса PIN**

Добавить импорт в верх файла:
```jsx
import PinGateModal from "@/shared/components/PinGateModal";
```

Добавить состояние после существующих `useAppStore` calls:
```jsx
const adultPinHash   = settings.adultPinHash ?? null;
const [pinResetMode, setPinResetMode] = useState(null); // null | "verify-old" | "set-new"
```

Добавить хэндлеры:
```jsx
function startPinReset() {
  setPinResetMode(adultPinHash === null ? "set-new" : "verify-old");
}

function handleVerifyOldSuccess() {
  setPinResetMode("set-new");
}

async function handleSetNewPin(hash) {
  await handlePatchSettings({ adultPinHash: hash });
}

function handleSetNewSuccess() {
  setPinResetMode(null);
}
```

Добавить кнопку в JSX рядом с другими настройками (после блока adultConfirmAdvance):
```jsx
<div className="settings-row">
  <span className="settings-label">PIN-код занятия</span>
  <button className="link-btn" onClick={startPinReset}>
    {adultPinHash ? "Изменить PIN" : "Задать PIN"}
  </button>
</div>
```

Добавить модалки в JSX (перед закрывающим `</div>`):
```jsx
{pinResetMode === "verify-old" && (
  <PinGateModal
    pinHash={adultPinHash}
    onSuccess={handleVerifyOldSuccess}
    onSetPin={() => {}}
  />
)}
{pinResetMode === "set-new" && (
  <PinGateModal
    pinHash={null}
    onSuccess={handleSetNewSuccess}
    onSetPin={handleSetNewPin}
  />
)}
```

- [ ] **Step 3: Commit**

```bash
git add src/features/settings/SettingsScreen.jsx
git commit -m "feat(settings): add PIN change/reset via settings screen"
```

---

## Финальная проверка

- [ ] Запустить все тесты: `npx vitest run` — ожидается PASS
- [ ] Запустить dev-сервер: `npm run dev`
- [ ] Проверить флоу первого запуска (PIN не задан → создать PIN → сессия стартует)
- [ ] Проверить повторный запуск (ввести PIN → сессия)
- [ ] Проверить неверный PIN (shake + сообщение об ошибке)
- [ ] Проверить что «Завершить» на итогах — обычная кнопка (нет тап-кода)
- [ ] Проверить что «Завершить» в exit-модалке — только hold (без тапов)
- [ ] Проверить сброс PIN через Settings
- [ ] Задеплоить: `npm run deploy:prod`
